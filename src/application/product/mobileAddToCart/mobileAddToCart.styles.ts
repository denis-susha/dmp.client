import styled from "styled-components";

const Container = styled.div`
    position: fixed;
    right: 0;
    bottom: 0;
    left: 0;
    z-index: 401;
    background: #fff;
    padding: 1rem 0.5rem;
`;

export const Styled = {
    Container,
};
